<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/title_activity_record_payment"
        android:textSize="24sp"
        android:layout_marginBottom="16dp"/>

    <Spinner
        android:id="@+id/spinner_tenant"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:prompt="@string/select_tenant_prompt"
        android:minHeight="48dp"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/et_payment_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/payment_date_hint"
        android:inputType="date"
        android:minHeight="48dp"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/et_payment_method"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/payment_method_hint"
        android:inputType="textCapWords"
        android:minHeight="48dp"
        android:layout_marginBottom="12dp"/>

    <EditText
        android:id="@+id/et_amount_paid"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/amount_paid_hint"
        android:inputType="numberDecimal"
        android:minHeight="48dp"
        android:layout_marginBottom="12dp"/>

    <Spinner
        android:id="@+id/spinner_invoice"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:prompt="@string/select_invoice_prompt"
        android:minHeight="48dp"
        android:layout_marginBottom="16dp"/>
        <!-- This will be populated later based on selected tenant -->

    <Button
        android:id="@+id/btn_save_payment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="48dp"
        android:text="@string/save_payment"
        android:textSize="18sp"/>

</LinearLayout>
